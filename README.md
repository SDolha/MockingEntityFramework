# MockingEntityFramework
Shows how to refactor services that use Entity Framework (DbContext, DbSet classes) to be able to unit test them.

Check out the second commit vs. the initial commit to see the changes.
